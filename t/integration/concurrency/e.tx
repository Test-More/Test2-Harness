use Test2::V0;
use TestBarrier qw/barrier_wait/;

# Do not finish until the run has as many of these going at once as it was
# told to allow, so the log has a real overlap to measure.
barrier_wait();

ok(1, "pass");

done_testing;
